#ifndef OPCODES_HPP
#define OPCODES_HPP
#include <cstddef>
#include <string>
#include <map>
#include <initializer_list>
template <size_t num> 
class map  {
    int8_t arr[num] ;

    int8_t operator[](std::string mnemonic){

    };
    map(std::initializer_list<> ){

    }; 
};
#endif